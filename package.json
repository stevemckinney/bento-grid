{
  "name": "bento-grid",
  "version": "1.0.0",
  "description": "A bento grid using tailwind, CSS grid and @container queries",
  "scripts": {
    "build": "npx @tailwindcss/cli -i app.css -o app.output.css --minify",
    "dev": "npx @tailwindcss/cli -i app.css -o app.output.css --watch"
  },
  "author": "Steve McKinney",
  "license": "ISC",
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  }
}
